<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Buitjes" parent="@android:style/Theme.Material.NoActionBar">
        <!-- The same near-black the web app uses behind its charts. -->
        <item name="android:windowBackground">#FF0A0C11</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
