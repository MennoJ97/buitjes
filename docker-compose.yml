services:
  ingestor:
    build:
      context: .
      dockerfile: ingestor/Dockerfile
    container_name: stratus-ingestor
    restart: unless-stopped
    env_file:
      - .env
    environment:
      FRAME_DIR: /data
    volumes:
      - frames:/data
    networks:
      - default

  weather-app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: stratus-weather-app
    restart: unless-stopped
    environment:
      FRAME_DIR: /data
      # Comma-separated origins allowed to call the API cross-origin. Unset
      # means same-origin only; the homepage widget's origin must be named.
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-}
      # Comma-separated keys for the widget endpoints. Empty leaves them open.
      API_KEYS: ${API_KEYS:-}
      API_KEY_PROTECTED_PREFIXES: ${API_KEY_PROTECTED_PREFIXES:-/api/point,/api/current}
    volumes:
      # Read-only: the ingestor owns this directory, the server only serves it.
      - frames:/data:ro
    depends_on:
      - ingestor
    # The frontend is baked into the image; no bind mount in production.
    # For local development use:
    #   docker compose -f docker-compose.yml -f docker-compose.dev.yml up
    ports:
      - "3000:3000"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.weatherapp.rule=Host(`weather.yourdomain.com`)" # Adjust to your domain
      - "traefik.http.routers.weatherapp.entrypoints=websecure"
      - "traefik.http.routers.weatherapp.tls=true"
      - "traefik.http.routers.weatherapp.tls.certresolver=myresolver" # Change to your Traefik cert resolver
      - "traefik.http.services.weatherapp.loadbalancer.server.port=3000"
      # Rate limiting lives here rather than in the app: it runs before a
      # request reaches the container, and it protects the static frontend and
      # the frame downloads too, which are the bandwidth-heavy part.
      # average = sustained requests/second per client IP, burst = allowance.
      # A map page load fetches ~85 frames at once, hence the generous burst.
      - "traefik.http.middlewares.weatherapp-ratelimit.ratelimit.average=20"
      - "traefik.http.middlewares.weatherapp-ratelimit.ratelimit.burst=120"
      - "traefik.http.middlewares.weatherapp-ratelimit.ratelimit.period=1s"
      - "traefik.http.routers.weatherapp.middlewares=weatherapp-ratelimit@docker"
    networks:
      - proxy
      - default

volumes:
  frames:

networks:
  proxy:
    external: true
  default: {}
